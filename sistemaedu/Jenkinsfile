pipeline {
    agent any
    triggers {
        pollSCM('* * * * *')
    }
    stages {

        stage("Compile") {
            steps {
                sh "./sistemaEdu/gradlew.bat compileJava"
            }
        }
        stage("Unit test") {
            steps {
                sh "./sistemaEdu/gradlew.bat test"
            }
        }
        stage("Code coverage") {
            steps {
        	    sh "./gradlew jacocoTestReport"
        	 	publishHTML (target: [
         	        reportDir: 'build/reports/jacoco/test/html',
         			reportFiles: 'index.html',
         			reportName: 'JacocoReport'
         	    ])
         		sh "./sistemaEdu/gradlew.bat jacocoTestCoverageVerification"
         	}
        }
        stage('SonarQube analysis') {
            steps {
                withSonarQubeEnv('SonarQubePruebas') {
                    sh './sistemaEdu/gradlew.bat sonarqube'
                }
            }
        }
    }
}